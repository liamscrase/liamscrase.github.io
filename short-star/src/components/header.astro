---
const siteTitle = "Liam Scrase";
---
		<nav class="desktop-navbar">
			<div class="desktop-navbar-container">
				<a href="/">Liam Scrase</a>
				<div class="nav-links">
					<ul>
						<li><a href="/about/">About</a></li>
						<li><a href="/contact/">Contact</a></li>
					</ul>
				</div>
			</div>
		</nav>
		
		<nav class="mobile-navbar">
			<input type="checkbox" id="menu-toggle" class="menu-toggle" />
			<div class="mobile-navbar-container">
				<a href="/">Liam Scrase</a>
				<label for="menu-toggle" class="hamburger">
					<span></span>
					<span></span>
					<span></span>
				</label>
			</div>
			<div class="offcanvas-menu">
				<div class="menu-items">
					<ul>
						<li><a href="/">Home</a></li>
						<li><a href="/about/">About</a></li>
						<li><a href="/contact/">Contact</a></li>
					</ul>
				</div>
			</div>
			<label for="menu-toggle" class="offcanvas-backdrop"></label>
		</nav>
<script>
	// @ts-nocheck
document.addEventListener("DOMContentLoaded", () => {
  const header = document.querySelector("header");
  const navs = document.querySelectorAll("nav"); // Select ALL nav elements
  const texts = document.querySelectorAll("#projects .text");

  // Set scroll threshold based on whether header exists
  const stickyNav = header ? header.offsetHeight : 50; // Use small threshold (50px) if no header

  window.addEventListener("scroll", () => {
    // Apply styles immediately on scroll for all pages
    if (window.scrollY > 10) { // Very small threshold for immediate effect
      navs.forEach(nav => nav.classList.add("scrolled")); // Apply to all navs
    } else {
      navs.forEach(nav => nav.classList.remove("scrolled")); // Remove from all navs
    }

    // // Text reveal animation
    // const windowBottom = window.scrollY + window.innerHeight;
    // texts.forEach(text => {
    //   const rect = text.getBoundingClientRect();
    //   const top = rect.top + window.scrollY;
    //   const bottomOfObject = top + rect.height - 100;

    //   if (windowBottom > bottomOfObject) {
    //     text.classList.add("text-reveal");
    //   }
    // });
  });
});
</script>
